@model LS_Shop.ViewModels.BestsellersViewModel

<div class="bestsellers">
    <h3 class="text-center">Bestsellery</h3> <br />
    <ul>
        @foreach (var bestseller in Model.Products)
        {
            @Html.Partial("ProductCard", bestseller)
        }
    </ul>
</div>